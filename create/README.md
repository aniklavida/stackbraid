# create — planned

**Status: not implemented.** This folder exists so the repository layout matches
[docs/STRUCTURE.md](../docs/STRUCTURE.md); it holds no working code yet.

`create` is the picker: a script that copies the chosen backend, database,
frontend and mobile pieces into a new project and writes their configuration.
It is not a code generator. See [docs/ROADMAP.md](../docs/ROADMAP.md), step 7:
it ships as an in-repo script first, published as an `npx` package at v1.0.

Do not treat the presence of this folder as evidence of a working picker.
