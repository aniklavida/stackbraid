# Copy to .env.local and point it at whichever backend you're running —
# the app never hard-codes a host anywhere else. Works unchanged against
# any backend that implements contract/openapi.yaml; see that backend's
# own README for how to start it against a local Postgres cluster.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8080
