// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'http://localhost:8080' | (string & {});
};

/**
 * RFC 3339, UTC, always suffixed `Z` — never a numeric offset such as `+00:00`. A spike caught one backend emitting the offset form while another emitted `Z` for the same instant; this pattern makes the offset form fail validation instead of merely looking inconsistent.
 */
export type UtcDateTime = string;

/**
 * Same rule as `UtcDateTime`; `null` means the event has not happened yet.
 */
export type NullableUtcDateTime = string | null;

/**
 * The error envelope for every non-2xx response, served as `application/problem+json` per RFC 9457. `type`, `title` and `status` are the RFC's required core; `code`, `traceId` and `errors` are this contract's extension members and are what clients actually branch on — `title` is for a human, `code` is for a switch statement.
 */
export type Problem = {
    /**
     * A stable identifier for this problem type. `about:blank` when no more specific type applies.
     */
    type: string;
    /**
     * A short, human-readable summary, constant for a given `type`.
     */
    title: string;
    /**
     * The HTTP status code, repeated here so it survives proxies that only pass the body along.
     */
    status: number;
    /**
     * A human-readable explanation specific to this occurrence.
     */
    detail?: string;
    /**
     * The request path that produced this problem.
     */
    instance?: string;
    /**
     * A stable, machine-readable application error code, e.g. `IDENTITY.INVALID_CREDENTIALS`. Stable across locales and across both backends; `title` and `detail` are not (they are localized).
     */
    code?: string;
    /**
     * Correlation ID for this request, matching the one in structured logs.
     */
    traceId?: string;
    /**
     * Present only for validation problems (`status` 400). Maps a field name to its violation messages.
     */
    errors?: {
        [key: string]: Array<string>;
    };
};

export type UserStatus = 'active' | 'inactive';

export type Role = {
    id: string;
    /**
     * Unique, human-readable role name, e.g. `admin`.
     */
    name: string;
    description?: string | null;
    /**
     * Permission codes granted by this role, e.g. `users:read`, `users:write`.
     */
    permissions: Array<string>;
};

export type User = {
    id: string;
    email: string;
    displayName: string;
    status: UserStatus;
    roles: Array<Role>;
    createdAt: UtcDateTime;
    updatedAt: UtcDateTime;
    lastLoginAt?: NullableUtcDateTime;
    /**
     * Set when the account is soft-deleted; null for an active account.
     */
    deletedAt: NullableUtcDateTime;
};

export type RegisterRequest = {
    email: string;
    displayName: string;
};

export type LoginRequest = {
    email: string;
};

/**
 * Optional. Provide `refreshToken` here for clients without a cookie jar (e.g. Flutter, backed by platform secure storage); omit entirely to use the httpOnly cookie instead (the browser path). Both forms are accepted by the same endpoint.
 */
export type RefreshRequest = {
    refreshToken?: string;
};

/**
 * Every field is optional; only the fields present are changed.
 */
export type UpdateUserRequest = {
    displayName?: string;
    email?: string;
};

export type AssignRoleRequest = {
    roleId: string;
};

export type DevicePlatform = 'ios' | 'android' | 'web';

export type RegisterDeviceTokenRequest = {
    platform: DevicePlatform;
    appVersion?: string;
};

export type RefreshDeviceTokenRequest = {
    token: string;
};

export type DeviceToken = {
    id: string;
    platform: DevicePlatform;
    appVersion: string;
    registeredAt: UtcDateTime;
    updatedAt: UtcDateTime;
};

export type Notification = {
    id: string;
    title: string;
    body: string;
    read: boolean;
    createdAt: UtcDateTime;
};

/**
 * Page<Notification>.
 */
export type NotificationPage = Page & {
    items: Array<Notification>;
};

export type TokenPair = {
    accessToken: string;
    refreshToken: string;
    tokenType: 'Bearer';
    /**
     * Expiry of `accessToken` (not the refresh token).
     */
    expiresAt: UtcDateTime;
};

/**
 * The shape every paginated list shares. OpenAPI has no generics, so `Page<T>` is realized as this base object plus a per-type `items` array — see `UserPage` below and `contract/README.md` for why.
 */
export type Page = {
    page: number;
    pageSize: number;
    totalItems: number;
    totalPages: number;
};

export type AuditEntry = {
    id: string;
    entityType: string;
    entityId: string;
    action: string;
    actorId?: string | null;
    correlationId: string;
    occurredAt: UtcDateTime;
    details?: string | null;
};

export type AuditPage = {
    items: Array<AuditEntry>;
};

/**
 * Page<User>.
 */
export type UserPage = Page & {
    items: Array<User>;
};

/**
 * Every message either backend pushes over realtime, whatever the transport. Each backend's own realtime mechanism differs in how a client subscribes; the JSON on the wire is this schema, identical on both. See the `x-realtime-channels` section of this document for the subscription mechanics.
 */
export type RealtimeMessage = ({
    type: 'user.deactivated';
} & UserDeactivatedMessage) | ({
    type: 'user.role_changed';
} & UserRoleChangedMessage) | ({
    type: 'job.progress';
} & JobProgressMessage);

export type UserDeactivatedMessage = {
    type: 'user.deactivated';
    userId: string;
    occurredAt: UtcDateTime;
};

export type UserRoleChangedMessage = {
    type: 'user.role_changed';
    userId: string;
    /**
     * The user's full role set after the change, not a diff.
     */
    roles: Array<Role>;
    occurredAt: UtcDateTime;
};

export type JobProgressMessage = {
    type: 'job.progress';
    jobId: string;
    status: 'queued' | 'running' | 'succeeded' | 'failed';
    progress: number;
    occurredAt: UtcDateTime;
};

export type RegisterRequestWritable = {
    email: string;
    password: string;
    displayName: string;
};

export type LoginRequestWritable = {
    email: string;
    password: string;
};

export type RegisterDeviceTokenRequestWritable = {
    token: string;
    platform: DevicePlatform;
    appVersion?: string;
};

export type UserIdParam = string;

export type RoleIdParam = string;

export type DeviceIdParam = string;

/**
 * 1-based page number.
 */
export type PageParam = number;

/**
 * Items per page.
 */
export type PageSizeParam = number;

/**
 * One sortable field, optionally prefixed with `-` for descending.
 * Allowed fields: `email`, `displayName`, `createdAt`, `status`.
 * Examples: `createdAt`, `-createdAt`.
 *
 */
export type UserSortParam = string;

/**
 * Case-insensitive substring match against email and display name.
 */
export type SearchParam = string;

export type RegisterUserData = {
    body: RegisterRequestWritable;
    path?: never;
    query?: never;
    url: '/v1/auth/register';
};

export type RegisterUserErrors = {
    /**
     * The request failed validation. `Problem.errors` maps each invalid field to its messages.
     */
    400: Problem;
    /**
     * The email address is already registered.
     */
    409: Problem;
};

export type RegisterUserError = RegisterUserErrors[keyof RegisterUserErrors];

export type RegisterUserResponses = {
    /**
     * Account created. The caller still has to log in separately — registration does not issue a session.
     */
    201: User;
};

export type RegisterUserResponse = RegisterUserResponses[keyof RegisterUserResponses];

export type LoginData = {
    body: LoginRequestWritable;
    path?: never;
    query?: never;
    url: '/v1/auth/login';
};

export type LoginErrors = {
    /**
     * The request failed validation. `Problem.errors` maps each invalid field to its messages.
     */
    400: Problem;
    /**
     * Invalid email or password.
     */
    401: Problem;
    /**
     * Rate limit exceeded. The response always uses the common Problem body with code IDENTITY.RATE_LIMITED.
     */
    429: Problem;
};

export type LoginError = LoginErrors[keyof LoginErrors];

export type LoginResponses = {
    /**
     * Authenticated. The body carries both tokens for non-browser clients
     * (Flutter has no ambient cookie jar). Browser clients additionally
     * receive the refresh token as an httpOnly, Secure, SameSite=Strict
     * cookie and should prefer that cookie over reading it from the body.
     *
     */
    200: TokenPair;
};

export type LoginResponse = LoginResponses[keyof LoginResponses];

export type RefreshTokenData = {
    /**
     * This endpoint accepts the refresh token via either form. Send it as `refreshToken` in this JSON body — the path for clients with no cookie jar, such as Flutter, backed by platform secure storage — or omit the body entirely to fall back to the httpOnly `refreshToken` cookie (the browser path). One endpoint serves both kinds of client.
     */
    body?: RefreshRequest;
    path?: never;
    query?: never;
    url: '/v1/auth/refresh';
};

export type RefreshTokenErrors = {
    /**
     * The refresh token is missing, expired, or already used.
     */
    401: Problem;
};

export type RefreshTokenError = RefreshTokenErrors[keyof RefreshTokenErrors];

export type RefreshTokenResponses = {
    /**
     * A new token pair. The old refresh token is revoked (rotation).
     */
    200: TokenPair;
};

export type RefreshTokenResponse = RefreshTokenResponses[keyof RefreshTokenResponses];

export type LogoutData = {
    /**
     * Omit the body to revoke the session named by the httpOnly cookie.
     */
    body?: RefreshRequest;
    path?: never;
    query?: never;
    url: '/v1/auth/logout';
};

export type LogoutErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
};

export type LogoutError = LogoutErrors[keyof LogoutErrors];

export type LogoutResponses = {
    /**
     * Session revoked. Idempotent — logging out twice is not an error.
     */
    204: void;
};

export type LogoutResponse = LogoutResponses[keyof LogoutResponses];

export type GetCurrentUserData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/v1/auth/me';
};

export type GetCurrentUserErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
};

export type GetCurrentUserError = GetCurrentUserErrors[keyof GetCurrentUserErrors];

export type GetCurrentUserResponses = {
    /**
     * The authenticated user.
     */
    200: User;
};

export type GetCurrentUserResponse = GetCurrentUserResponses[keyof GetCurrentUserResponses];

export type RegisterDeviceTokenData = {
    body: RegisterDeviceTokenRequestWritable;
    path?: never;
    query?: never;
    url: '/v1/devices';
};

export type RegisterDeviceTokenErrors = {
    /**
     * The request failed validation. `Problem.errors` maps each invalid field to its messages.
     */
    400: Problem;
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The token is already registered to another user.
     */
    409: Problem;
};

export type RegisterDeviceTokenError = RegisterDeviceTokenErrors[keyof RegisterDeviceTokenErrors];

export type RegisterDeviceTokenResponses = {
    /**
     * The device token is registered and is now eligible for push delivery.
     */
    201: DeviceToken;
};

export type RegisterDeviceTokenResponse = RegisterDeviceTokenResponses[keyof RegisterDeviceTokenResponses];

export type RevokeDeviceTokenData = {
    body?: never;
    path: {
        deviceId: string;
    };
    query?: never;
    url: '/v1/devices/{deviceId}';
};

export type RevokeDeviceTokenErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * No resource exists at this identifier.
     */
    404: Problem;
};

export type RevokeDeviceTokenError = RevokeDeviceTokenErrors[keyof RevokeDeviceTokenErrors];

export type RevokeDeviceTokenResponses = {
    /**
     * Revoked, or the token was already absent; both are treated as success.
     */
    204: void;
};

export type RevokeDeviceTokenResponse = RevokeDeviceTokenResponses[keyof RevokeDeviceTokenResponses];

export type RefreshDeviceTokenData = {
    body: RefreshDeviceTokenRequest;
    path: {
        deviceId: string;
    };
    query?: never;
    url: '/v1/devices/{deviceId}';
};

export type RefreshDeviceTokenErrors = {
    /**
     * The request failed validation. `Problem.errors` maps each invalid field to its messages.
     */
    400: Problem;
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * No resource exists at this identifier.
     */
    404: Problem;
};

export type RefreshDeviceTokenError = RefreshDeviceTokenErrors[keyof RefreshDeviceTokenErrors];

export type RefreshDeviceTokenResponses = {
    /**
     * The device token was refreshed.
     */
    200: DeviceToken;
};

export type RefreshDeviceTokenResponse = RefreshDeviceTokenResponses[keyof RefreshDeviceTokenResponses];

export type ListNotificationsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * 1-based page number.
         */
        page?: number;
        /**
         * Items per page.
         */
        pageSize?: number;
    };
    url: '/v1/notifications';
};

export type ListNotificationsErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
};

export type ListNotificationsError = ListNotificationsErrors[keyof ListNotificationsErrors];

export type ListNotificationsResponses = {
    /**
     * A page of in-app notifications.
     */
    200: NotificationPage;
};

export type ListNotificationsResponse = ListNotificationsResponses[keyof ListNotificationsResponses];

export type ListUsersData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * 1-based page number.
         */
        page?: number;
        /**
         * Items per page.
         */
        pageSize?: number;
        /**
         * One sortable field, optionally prefixed with `-` for descending.
         * Allowed fields: `email`, `displayName`, `createdAt`, `status`.
         * Examples: `createdAt`, `-createdAt`.
         *
         */
        sort?: string;
        /**
         * Case-insensitive substring match against email and display name.
         */
        search?: string;
        /**
         * Filter by account status.
         */
        status?: UserStatus;
        /**
         * Filter to users holding this role.
         */
        roleId?: string;
        /**
         * Include soft-deleted users. Defaults to false.
         */
        includeDeleted?: boolean;
    };
    url: '/v1/users';
};

export type ListUsersErrors = {
    /**
     * The request failed validation. `Problem.errors` maps each invalid field to its messages.
     */
    400: Problem;
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
};

export type ListUsersError = ListUsersErrors[keyof ListUsersErrors];

export type ListUsersResponses = {
    /**
     * A page of users.
     */
    200: UserPage;
};

export type ListUsersResponse = ListUsersResponses[keyof ListUsersResponses];

export type GetUserData = {
    body?: never;
    path: {
        userId: string;
    };
    query?: {
        /**
         * Include a soft-deleted user for administrative recovery.
         */
        includeDeleted?: boolean;
    };
    url: '/v1/users/{userId}';
};

export type GetUserErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
    /**
     * No resource exists at this identifier.
     */
    404: Problem;
};

export type GetUserError = GetUserErrors[keyof GetUserErrors];

export type GetUserResponses = {
    /**
     * The requested user.
     */
    200: User;
};

export type GetUserResponse = GetUserResponses[keyof GetUserResponses];

export type UpdateUserData = {
    body: UpdateUserRequest;
    path: {
        userId: string;
    };
    query?: never;
    url: '/v1/users/{userId}';
};

export type UpdateUserErrors = {
    /**
     * The request failed validation. `Problem.errors` maps each invalid field to its messages.
     */
    400: Problem;
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
    /**
     * No resource exists at this identifier.
     */
    404: Problem;
    /**
     * The new email address is already in use by another account.
     */
    409: Problem;
};

export type UpdateUserError = UpdateUserErrors[keyof UpdateUserErrors];

export type UpdateUserResponses = {
    /**
     * The updated user.
     */
    200: User;
};

export type UpdateUserResponse = UpdateUserResponses[keyof UpdateUserResponses];

export type DeactivateUserData = {
    body?: never;
    path: {
        userId: string;
    };
    query?: never;
    url: '/v1/users/{userId}/deactivate';
};

export type DeactivateUserErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
    /**
     * No resource exists at this identifier.
     */
    404: Problem;
};

export type DeactivateUserError = DeactivateUserErrors[keyof DeactivateUserErrors];

export type DeactivateUserResponses = {
    /**
     * The user, now inactive.
     */
    200: User;
};

export type DeactivateUserResponse = DeactivateUserResponses[keyof DeactivateUserResponses];

export type RestoreUserData = {
    body?: never;
    path: {
        userId: string;
    };
    query?: never;
    url: '/v1/users/{userId}/restore';
};

export type RestoreUserErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
    /**
     * No resource exists at this identifier.
     */
    404: Problem;
};

export type RestoreUserError = RestoreUserErrors[keyof RestoreUserErrors];

export type RestoreUserResponses = {
    /**
     * The restored, active user.
     */
    200: User;
};

export type RestoreUserResponse = RestoreUserResponses[keyof RestoreUserResponses];

export type ListUserAuditData = {
    body?: never;
    path?: never;
    query: {
        userId: string;
    };
    url: '/v1/audit';
};

export type ListUserAuditErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
};

export type ListUserAuditError = ListUserAuditErrors[keyof ListUserAuditErrors];

export type ListUserAuditResponses = {
    /**
     * Audit events for the requested user, newest first.
     */
    200: AuditPage;
};

export type ListUserAuditResponse = ListUserAuditResponses[keyof ListUserAuditResponses];

export type ListRolesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/v1/roles';
};

export type ListRolesErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
};

export type ListRolesError = ListRolesErrors[keyof ListRolesErrors];

export type ListRolesResponses = {
    /**
     * Every role in the system.
     */
    200: Array<Role>;
};

export type ListRolesResponse = ListRolesResponses[keyof ListRolesResponses];

export type AssignRoleData = {
    body: AssignRoleRequest;
    path: {
        userId: string;
    };
    query?: never;
    url: '/v1/users/{userId}/roles';
};

export type AssignRoleErrors = {
    /**
     * The request failed validation. `Problem.errors` maps each invalid field to its messages.
     */
    400: Problem;
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
    /**
     * The user or the role does not exist.
     */
    404: Problem;
};

export type AssignRoleError = AssignRoleErrors[keyof AssignRoleErrors];

export type AssignRoleResponses = {
    /**
     * The user, with the role now attached. Assigning a role the user already holds is a no-op that returns the same state.
     */
    200: User;
};

export type AssignRoleResponse = AssignRoleResponses[keyof AssignRoleResponses];

export type RevokeRoleData = {
    body?: never;
    path: {
        userId: string;
        roleId: string;
    };
    query?: never;
    url: '/v1/users/{userId}/roles/{roleId}';
};

export type RevokeRoleErrors = {
    /**
     * The access token is missing, expired, or invalid.
     */
    401: Problem;
    /**
     * The caller is authenticated but lacks the permission this endpoint requires.
     */
    403: Problem;
    /**
     * The user or the role does not exist.
     */
    404: Problem;
};

export type RevokeRoleError = RevokeRoleErrors[keyof RevokeRoleErrors];

export type RevokeRoleResponses = {
    /**
     * Revoked, or the user did not hold the role to begin with — both are treated as success.
     */
    204: void;
};

export type RevokeRoleResponse = RevokeRoleResponses[keyof RevokeRoleResponses];
